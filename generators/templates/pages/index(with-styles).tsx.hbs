import * as S from './styles';

const {{pascalCase (getName name)}} = () => {
  return (
    <S.Wrapper>
      <h1>{{pascalCase (getName name)}}</h1>
    </S.Wrapper>
  );
};

export default {{pascalCase (getName name)}};
