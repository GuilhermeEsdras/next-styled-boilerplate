import { render } from '@testing-library/react';
// import { renderWithTheme } from '@src/utils/tests/helpers';

import {{pascalCase (getName name)}} from './';

describe('<{{pascalCase (getName name)}} /> Snapshot Matches', () => {
  it('renders {{pascalCase (getName name)}} unchanged', () => {
    const { container } = render(<{{pascalCase (getName name)}} />);
    // const { container } = renderWithTheme(<{{pascalCase (getName name)}} />);

    expect(container.firstChild).toMatchSnapshot();
  });
});
