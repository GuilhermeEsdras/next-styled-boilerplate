import { screen } from '@testing-library/react';

import { renderWithTheme } from '@src/utils/tests/helpers';

import {{pascalCase (getName name)}} from './{{pascalCase (getName name)}}';

describe('<{{pascalCase (getName name)}} /> Tests', () => {
  it('should render the heading', () => {
    renderWithTheme(<{{pascalCase (getName name)}} />);

    expect(
      screen.getByRole('heading', { name: /{{pascalCase (getName name)}}/i })
    ).toBeInTheDocument();
  });
});
